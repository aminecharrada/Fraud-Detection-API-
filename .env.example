# Runtime threshold (overrides any default in code)
THRESHOLD=0.3086
# Optional: port binding for Flask/Gunicorn
PORT=5000
# Optional: path to model folder or bundle file
MODEL_DIR=./model
MODEL_BUNDLE=